import { Category } from './category.interface';

export class Product {
    id?: number;
    name?: string;
    description?: string;
    price?: string;
    weight?: string;
    categoryId?: number;
  }